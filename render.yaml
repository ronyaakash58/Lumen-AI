services:
  - type: web
    name: lumen-ai
    env: node
    plan: starter
    branch: main
    buildCommand: yarn install && yarn build
    startCommand: yarn start
    autoDeploy: true
    regions:
      - oregon
